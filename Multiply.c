#include <stdio.h>
#include "Multiply.h"
/**
 * main - program to print multiplication
 * of numbers in c programming
 *
 * Return: Always 0. (Success)
 */

int main(void)
{
	int result = MULTIPLY (20, 3);

	printf("The final Test result is as follows: %d\n", result);

	return(0);
}
