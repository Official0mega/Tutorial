#include <stdio.h>

/**
 * main - if and else statement 1.
 *
 * Return: NULL
 */

int main(void)
{
		int age = 18;

		if (age >= 25)
		{
			printf("You are Eligible to Proceed");
		}

		else
		{
			printf("Sorry, you're not allowed");
		}
		
		return(0);
}
